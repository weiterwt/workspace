#include "MyVector.h"

/*template <typename Object>
MyVector<Object>::MyVector(int _size = 0) : the_size(_size),
	the_capacity(_size + DEFAULT_CAPACITY)
{
	ptr = new Object[_size + DEFAULT_CAPACITY];
}
template <typename Object>
MyVector<Object>::~MyVector()
{
	delete [] ptr;
}
template <typename Object>
MyVector<Object>::MyVector(const MyVector &other)
{
	if (*this == other);
}
template <typename Object>
bool MyVector<Object>::empty() const
{
	if (the_size == 0)
		return true;
	return false;
}
template <typename Object>
size_t MyVector<Object>::size() const
{
	return the_size;
}*/
